# REST base URL your axios client calls (see Swagger "Servers" / paths like /api/auth/...).
# Do NOT use the docs page URL (e.g. .../api-docs/) here—that is Swagger UI only.
# Dev example: VITE_API_URL=http://localhost:5000/api
VITE_API_URL=https://your-backend.example.com/api
